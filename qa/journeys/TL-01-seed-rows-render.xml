<journey name="TL-01 The four seed rows render with title, priority and a complete control">
  <description>
    Covers FR-01. REQUIREMENTS.md section 8 fixes the seed data and section 9 precedence 4 says a
    non-empty list is a LazyColumn of rows. Order here is TaskSort.DEFAULT, which holds the source
    order (BACKLOG.md Q-1).

    Priority is exposed only as a content description: PriorityIndicator wraps its label in
    clearAndSetSemantics (core/ui PriorityIndicator.kt:41), so "High" is never row text.

    Preconditions:
      adb shell am force-stop com.rounds.test.to_dolist
      adb shell am start -n com.rounds.test.to_dolist/.MainActivity --es qa_failure_rate 0.0
  </description>
  <actions>
    <action>Verify that "Renew domain registration" is on screen</action>
    <action>Verify that "Reply to design feedback" is on screen</action>
    <action>Verify that "Book dentist" is on screen</action>
    <action>Verify that "Migrate the analytics pipeline to the new warehouse and validate dashboards" is on screen</action>
    <action>Verify that the four titles appear in that order from top to bottom</action>
    <action>Verify that exactly four elements with the content description "Mark complete" are on screen</action>
    <action>Verify that exactly four elements with the content description "Delete task" are on screen</action>
    <action>Verify that an element with the content description "High" is on screen</action>
    <action>Verify that an element with the content description "Low" is on screen</action>
    <action>Verify that exactly two elements with the content description "Medium" are on screen</action>
  </actions>
</journey>
